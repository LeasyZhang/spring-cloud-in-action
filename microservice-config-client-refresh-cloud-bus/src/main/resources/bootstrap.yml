spring:
  application:
    name: microservice-client

  cloud:
    config:
      uri: http://localhost:8050/
      profile: dev
      label: master

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest